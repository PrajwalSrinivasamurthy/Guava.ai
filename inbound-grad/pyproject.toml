[project]
name = "inbound-grad"
version = "0.1.0"
requires-python = "==3.14.*"
dependencies = [
    "guava-sdk>=0.41.0",
    "typing_extensions",
    "websockets",
    "gsheets",
    "chat_notify",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "pytest",
    "agent_testing",
]

[tool.uv.sources]
gsheets = { path = "../patterns/gsheets" }
chat_notify = { path = "../patterns/chat_notify" }
agent_testing = { path = "../patterns/agent_testing", editable = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
